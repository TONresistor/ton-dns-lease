{
  "name": "dns-rent",
  "version": "1.0.0",
  "description": "TON DNS Lease V1 - Smart contracts for leasing TON DNS domain NFTs",
  "scripts": {
    "build": "mkdir -p build && npx tolk-js -o build/dns-rent-listing.json contracts/dns-rent-listing.tolk && npx tolk-js -o build/dns-rent-marketplace.json contracts/dns-rent-marketplace.tolk",
    "test": "vitest run"
  },
  "devDependencies": {
    "@ton/blueprint": "^0.44.0",
    "@ton/core": "^0.63.1",
    "@ton/crypto": "^3.3.0",
    "@ton/sandbox": "^0.41.0",
    "@ton/test-utils": "^0.12.0",
    "@ton/tolk-js": "^1.2.0",
    "vitest": "^4.1.0"
  }
}
